import fs from 'fs'
let handler = async (m, { conn, args, command }) => {
let totalf = Object.values(global.plugins).filter(
    (v) => v.help && v.tags
  ).length;
 await conn.sendButton(m.chat, `https://youtu.be/@vicenziprst`,wm + '\n\n' + botdate, giflogo, [['Ownef','.owner']],
                      }}
})
}


handler.help = ['ytowner']
handler.tags = ['info']
handler.command = ['ytowner']
export default handler
